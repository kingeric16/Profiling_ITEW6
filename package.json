{
  "name": "itew6",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run serve --prefix backend\" \"npm run dev --prefix frontend\"",
    "serve": "concurrently -n api,web -c blue,green \"npm run serve --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
